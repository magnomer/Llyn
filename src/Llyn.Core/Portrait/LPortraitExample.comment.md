# LPortraitExample.cs

## `public sealed record LPortraitExample`

One example line of a card, split the way the display splits it.
The citation is absent because the display card shows none.

**Parameters**

- `LPortraitExampleFrame` - the bracketed particle and dependence head, empty when neither is set.
- `LPortraitExampleText` - the sentence itself.
