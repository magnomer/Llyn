# LDiweiPage.cs

## `public sealed record LDiweiPage(`

The page of one Diwei category as the engine composes it: language, key, side, and the sorted sections.
The blank page stands for no category, so the veneer never branches on null.
