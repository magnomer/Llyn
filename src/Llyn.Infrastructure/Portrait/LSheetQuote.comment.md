# LSheetQuote.cs

## `public static class LSheetQuote`

Renders the example lines of one card.

## `public static void LSheetQuoteAppend(StringBuilder page, LPortraitCard card)`

The dot, the framed head and the sentence stand in three columns, as they do on screen.
The head is kept on one line, because it reads as a unit rather than as prose.
