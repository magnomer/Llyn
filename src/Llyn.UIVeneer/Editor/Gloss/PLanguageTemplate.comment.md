# PLanguageTemplate.xaml

The language selector row shared by editor cards.

## `Theme.Language.Row`

Shows a language flag and name, with an empty circle when no flag exists.
Clicking the row delegates speaker selection to the editor.
