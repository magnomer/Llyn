<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Llyn.UIVeneer">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Llyn;component/Display/Template/PDisplayState.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Display.Card.Register" TargetType="ItemsControl">
        <Setter Property="Margin" Value="{DynamicResource Theme.Card.RegisterMargin}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding LCardDraftRegister.Count}" Value="0">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Display.Card.RegisterLink" TargetType="Button">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="PSurface" Style="{DynamicResource Theme.Card.Register}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PSurface" Property="BorderBrush" Value="{DynamicResource Theme.Helper}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PSurface" Property="Opacity" Value="0.72" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="Display.Card.RegisterChip">
        <Button Style="{StaticResource Display.Card.RegisterLink}">
            <TextBlock FontSize="{DynamicResource Theme.Card.RegisterSize}" Foreground="{DynamicResource Theme.Helper}">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource Display.Card.State}">
                        <Binding Path="LRegisterDraftName" />
                        <local:PLocalizationBinder PLocalizationBinderKey="Display.Unknown" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Button>
    </DataTemplate>
</ResourceDictionary>
