# PStemCommand.cs

## `public static class PStemCommand`

The command a character of a series page is opened through.
A template raises it, so the page needs no handler of its own per character.

## `public static RoutedCommand PStemCommandEntry`

Asks for the entry written with the character the chip carries.
