# PThemeDefault.xaml

Applies the default canvas, ink, and font to windows and text blocks.

## `Window`

Sets the application canvas, foreground, font, and text rendering defaults.

## `TextBlock`

Uses the default ink color for text without an explicit foreground.
