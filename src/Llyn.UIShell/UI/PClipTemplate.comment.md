# PClipTemplate.xaml

## `<DataTemplate x:Key="Theme.Clip.Row">`

One recording: the source that offered it, small, the way a candidate names its source.
The recording itself has nothing to read, so the row says nothing about it — it is played.
The taking button carries this row's own download state, so a later arrival cannot overwrite it.
