# PLookupCandidate.cs

## `internal sealed class PLookupCandidate`

Presentation item for one pronunciation candidate shown in `PLookupMenu`. Wraps the domain `LCandidate` with the localized source label the menu row displays.
