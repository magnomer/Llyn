# PLangcodeIndicator.cs

## `internal static class PLangcodeIndicator`

Resolves a language pack's cached SVG flag into the frozen drawing used by both the editor's language picker and the read-only entry display. A malformed or unreadable flag becomes no image, which lets each surface show its neutral globe fallback.
