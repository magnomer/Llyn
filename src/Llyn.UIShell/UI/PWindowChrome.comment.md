# PWindowChrome.cs

## `public partial class PWindow`

The window frame the program draws for itself, since the system one is off: the caption buttons, dragging the window by its roof, and the maximize/restore toggle they share.
