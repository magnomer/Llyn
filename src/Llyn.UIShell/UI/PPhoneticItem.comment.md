# PPhoneticItem.cs

## `internal sealed class PPhoneticItem`

Presentation item for one pronunciation candidate shown in `PPhonetic`.
Wraps the domain `LCandidate` with the localized source label the menu row displays.
