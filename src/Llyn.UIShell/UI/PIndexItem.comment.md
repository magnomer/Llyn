# PIndexItem.cs

## `internal sealed class PIndexItem`

Presentation item for one entry row in `PIndex`. Carries the headword and language the row shows, and the entry id the row loads through — the id is identity and never displayed, so the row stays selectable after a headword is edited into something another entry also reads as.
