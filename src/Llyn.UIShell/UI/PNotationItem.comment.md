# PNotationItem.cs

## `internal sealed class PNotationItem`

Presentation item for one pronunciation candidate shown in `PNotation`.
Wraps the domain `LCandidate` with the localized source label the menu row displays.
