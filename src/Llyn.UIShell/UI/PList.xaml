<UserControl
    x:Class="Llyn.UIShell.PList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svgc="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf"
    IsVisibleChanged="PListHandle">

    <!--
        The read-only card shapes. A Meaning card and a Collocation card are one card (LCardDraft), so
        the two templates below differ in one element: the Collocation's Expression. Every member the
        loaded draft carries is drawn, and a field the card left empty collapses rather than leaving a
        blank line, which is what these styles are for.
    -->
    <UserControl.Resources>
        <Style x:Key="Display.Card.Title" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftTitle}" Value="">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Display.Card.Expression" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,3,0,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource Theme.Accent}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftExpression}" Value="">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Display.Card.Meaning" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,3,0,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftMeaning}" Value="">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Display.Card.Example" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,8,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftExample.Count}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Display.Card.Situation" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,8,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftSituation.Count}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Display.Card.Tag" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,8,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding LCardDraftTag.Count}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="Display.Card.Line">
            <TextBlock Margin="0,2,0,0" FontSize="13" Text="{Binding}" TextWrapping="Wrap" />
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="34,20,34,38">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- POrder belongs beside PInquiry in this row; ordering is a later job. -->
        <TextBox
            x:Name="PInquiry"
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Height="44"
            Margin="0,0,0,18"
            Style="{StaticResource Theme.Input.Field}"
            Tag="{DynamicResource List.Search}"
            TextChanged="PInquiryHandle" />

        <Border Grid.Row="1" Grid.Column="0" Margin="0,0,20,0" Style="{StaticResource Theme.Input.Card}">
            <Grid>
                <ScrollViewer Margin="10" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="PIndex">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Margin="0,0,0,6" Click="PIndexHandle" Style="{StaticResource Theme.Popup.Row}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" Text="{Binding PIndexItemHeadword}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource Theme.Muted}" Text="{Binding PIndexItemLanguage}" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <TextBlock
                    x:Name="PIndexEmpty"
                    Margin="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Foreground="{StaticResource Theme.Muted}"
                    IsHitTestVisible="False"
                    Text="{DynamicResource List.Empty}"
                    TextWrapping="Wrap" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Theme.Input.Card}">
            <Grid x:Name="PDisplay">
                <TextBlock
                    x:Name="PDisplayEmpty"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource Theme.Muted}"
                    Text="{DynamicResource List.Unselected}" />
                <ScrollViewer x:Name="PDisplayContents" Margin="34,28,34,28" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="PDisplayHeadword" VerticalAlignment="Bottom" FontSize="34" FontWeight="Bold" TextWrapping="Wrap" />
                            <TextBlock x:Name="PDisplayLanguage" Margin="14,0,0,8" VerticalAlignment="Bottom" FontSize="13" Foreground="{StaticResource Theme.Muted}" />
                        </StackPanel>
                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                            <TextBlock x:Name="PDisplayPronunciation" VerticalAlignment="Center" FontSize="16" Foreground="{StaticResource Theme.Accent}" Visibility="Collapsed" />
                            <!-- Shown only when the entry has an audio file on disk; see PDisplayShow. -->
                            <Button x:Name="PDisplayPlayback" Width="34" Height="34" Margin="12,0,0,0" Background="{StaticResource Theme.Surface}" BorderBrush="{StaticResource Theme.Line}" Foreground="{StaticResource Theme.Accent}" Click="PDisplayPlaybackHandle" Style="{StaticResource Theme.Input.Action}" Visibility="Collapsed">
                                <!-- Phosphor play.svg, tinted through an opacity mask (see PPlayback). -->
                                <Rectangle Width="11" Height="13" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform">
                                            <VisualBrush.Visual>
                                                <svgc:SvgViewbox Width="11" Height="13" Source="icons/play.svg" />
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </Button>
                        </StackPanel>

                        <TextBlock Margin="0,26,0,8" Style="{StaticResource Theme.Input.Label}" Text="{DynamicResource Display.Sense}" />
                        <ItemsControl x:Name="PDisplaySense">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,14">
                                        <TextBlock Style="{StaticResource Display.Card.Title}" Text="{Binding LCardDraftTitle}" />
                                        <TextBlock Style="{StaticResource Display.Card.Meaning}" Text="{Binding LCardDraftMeaning}" />
                                        <StackPanel Style="{StaticResource Display.Card.Example}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Example" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftExample}" />
                                        </StackPanel>
                                        <StackPanel Style="{StaticResource Display.Card.Situation}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Situation" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftSituation}" />
                                        </StackPanel>
                                        <StackPanel Style="{StaticResource Display.Card.Tag}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Tags" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftTag}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Margin="0,20,0,8" Style="{StaticResource Theme.Input.Label}" Text="{DynamicResource Display.Collocation}" />
                        <ItemsControl x:Name="PDisplayCollocation">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,14">
                                        <TextBlock Style="{StaticResource Display.Card.Title}" Text="{Binding LCardDraftTitle}" />
                                        <TextBlock Style="{StaticResource Display.Card.Expression}" Text="{Binding LCardDraftExpression}" />
                                        <TextBlock Style="{StaticResource Display.Card.Meaning}" Text="{Binding LCardDraftMeaning}" />
                                        <StackPanel Style="{StaticResource Display.Card.Example}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Example" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftExample}" />
                                        </StackPanel>
                                        <StackPanel Style="{StaticResource Display.Card.Situation}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Situation" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftSituation}" />
                                        </StackPanel>
                                        <StackPanel Style="{StaticResource Display.Card.Tag}">
                                            <TextBlock Style="{StaticResource Theme.Input.Label}" Text="Tags" />
                                            <ItemsControl ItemTemplate="{StaticResource Display.Card.Line}" ItemsSource="{Binding LCardDraftTag}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Margin="0,20,0,8" Style="{StaticResource Theme.Input.Label}" Text="{DynamicResource Display.Note}" />
                        <TextBlock x:Name="PDisplayNote" FontSize="14" TextWrapping="Wrap" Visibility="Collapsed" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
