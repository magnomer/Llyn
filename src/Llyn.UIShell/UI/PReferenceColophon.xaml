<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Llyn.UIShell">

    <Style x:Key="Reference.Colophon.Label" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Muted}" />
    </Style>

    <Style x:Key="Reference.Colophon.Value" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <DataTemplate x:Key="Reference.Footnote.Card">
        <Button Style="{DynamicResource Theme.Footnote.Row}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding PFootnoteItemName}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Margin="9,0,0,0" VerticalAlignment="Center" FontSize="11" Foreground="{DynamicResource Theme.Accent}" Text="{Binding PFootnoteItemKind}" />
                    </StackPanel>
                    <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="{DynamicResource Theme.Muted}" Text="{Binding PFootnoteItemTitle}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" Orientation="Horizontal">
                    <Image Width="19" Height="14" Margin="0,0,7,0" VerticalAlignment="Center" Source="{Binding PFootnoteItemFlag}" Stretch="Uniform" />
                    <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource Theme.Muted}" Text="{Binding PFootnoteItemLanguage}" />
                </StackPanel>
            </Grid>
        </Button>
    </DataTemplate>

</ResourceDictionary>
