<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Llyn.UIShell"
    x:Class="Llyn.UIShell.PCollocationTemplate">

    <local:PStateConverter x:Key="Theme.State.Show" />

    <DataTemplate x:Key="Theme.Collocation.Card">
        <Border Style="{DynamicResource Theme.Card.Shell}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="55" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Style="{DynamicResource Theme.Card.Header}" Cursor="SizeAll" MouseLeftButtonDown="PCardDragHandle">
                    <Grid Style="{DynamicResource Theme.Card.Crown}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{DynamicResource Theme.Card.Position}">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{DynamicResource Theme.Accent}" Text="{Binding PCardPosition}" />
                        </Border>
                        <TextBox Grid.Column="1" Margin="0,0,34,0" Style="{DynamicResource Theme.Card.Title}" Text="{Binding PTitle, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Tag>
                                <MultiBinding Converter="{StaticResource Theme.State.Show}">
                                    <Binding Path="PTitleUnreadable" />
                                    <Binding Path="PCardTitle" />
                                </MultiBinding>
                            </TextBox.Tag>
                        </TextBox>
                        <Button
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="PCardHandle"
                            Style="{DynamicResource Theme.Card.Handle}">
                            <Path Width="12" Height="12" Data="M 1,1 L 11,11 M 11,1 L 1,11" Stroke="{DynamicResource Theme.Muted}" StrokeEndLineCap="Round" StrokeThickness="1.5" />
                        </Button>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" Style="{DynamicResource Theme.Card.Body}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Style="{DynamicResource Theme.Card.Gutter}" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBox Style="{DynamicResource Theme.Card.Expression}" Text="{Binding PCardExpression, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Tag>
                                <MultiBinding Converter="{StaticResource Theme.State.Show}">
                                    <Binding Path="PCardExpressionUnreadable" />
                                    <local:PLocalizationBinder PLocalizationBinderKey="Display.Unreadable" />
                                    <local:PLocalizationBinder PLocalizationBinderKey="Card.ExpressionHint" />
                                </MultiBinding>
                            </TextBox.Tag>
                        </TextBox>

                        <TextBox Style="{DynamicResource Theme.Card.Meaning}" Text="{Binding PCardDefinition, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Tag>
                                <MultiBinding Converter="{StaticResource Theme.State.Show}">
                                    <Binding Path="PCardDefinitionUnreadable" />
                                    <local:PLocalizationBinder PLocalizationBinderKey="Display.Unreadable" />
                                    <local:PLocalizationBinder PLocalizationBinderKey="Card.MeaningHint" />
                                </MultiBinding>
                            </TextBox.Tag>
                        </TextBox>

                        <ItemsControl ItemTemplate="{DynamicResource Theme.Context.Row}" ItemsSource="{Binding PCardContext}" Style="{DynamicResource Theme.Card.Situation}" />

                        <ItemsControl ItemsSource="{Binding PCardLink}" Style="{DynamicResource Theme.Link.Field}" />

                        <Border Style="{DynamicResource Theme.Card.Example}">
                            <ItemsControl ItemTemplate="{DynamicResource Theme.Sentence.Row}" ItemsSource="{Binding PCardSentence}" />
                        </Border>

                        <ItemsControl ItemsSource="{Binding PCardLabel}" Style="{DynamicResource Theme.Label.Field}" />

                        <ItemsControl ItemTemplate="{DynamicResource Theme.Image.Row}" ItemsSource="{Binding PCardImage}" Style="{DynamicResource Theme.Card.Media}" />

                        <ItemsControl ItemTemplate="{DynamicResource Theme.Video.Row}" ItemsSource="{Binding PCardVideo}" Style="{DynamicResource Theme.Card.Media}" />

                        <StackPanel Margin="0,16,0,0" Orientation="Horizontal">
                            <Button Height="31" Padding="12,0" Click="PImageAddHandle" Style="{DynamicResource Theme.Input.Pill}">
                                <TextBlock VerticalAlignment="Center" FontSize="12" Text="{DynamicResource Card.ImageAdd}" />
                            </Button>
                            <Button Height="31" Margin="7,0,0,0" Padding="12,0" Click="PVideoAddHandle" Style="{DynamicResource Theme.Input.Pill}">
                                <TextBlock VerticalAlignment="Center" FontSize="12" Text="{DynamicResource Card.VideoAdd}" />
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>
