# PHeadword.cs

## `private void PHeadwordFontApply(string language)`

Draws the headword field in the typography its language declares.
The two hidden copies behind the field are drawn the same way.
They are what the field is measured against, so a size they do not share would misplace everything beside it.
