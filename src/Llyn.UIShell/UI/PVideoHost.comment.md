# PVideoHost.cs

## `internal interface PVideoHost`

What a video row's template asks of whoever drew it.
That is a file chosen and a row dropped, as for a picture.
The entry editor and the situation editor both implement it, so one template serves both.
Playing is not here, because the Screen answers for playing itself.
