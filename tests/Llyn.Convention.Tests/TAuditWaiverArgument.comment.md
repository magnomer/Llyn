# TAuditWaiverArgument.cs

## `internal static partial class TAuditTruthSetting`

The argument waivers, one part of the truth settings.
Hand-written and tracked, and only ever shortened.

## `public static readonly string[] TAuditWaiverArgument`

Each line is `File.cs:Name:Kind` for a hit the shell carried when custody was first enforced.
