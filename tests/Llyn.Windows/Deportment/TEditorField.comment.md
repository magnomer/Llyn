# TEditorField.cs

## `public sealed class TEditorField`

Covers the spine's field setters.
A headword set reaches the draft once persisted and makes it storable.
A note set drops its trailing line breaks.
An empty language is ignored.
Typing that echoes while the draft is being announced defers nothing.
