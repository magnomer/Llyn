# TCaretKey.cs

## `public sealed class TCaretKey`

The chip caret keys, driven by name with no window or text box.
Each case proves one branch of `LCaret.LCaretKeyApply` and the callbacks it reaches.
