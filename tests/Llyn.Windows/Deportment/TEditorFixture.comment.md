# TEditorFixture.cs

## `internal static class TEditorFixture`

The stored entry and the opened editor the editor tests share.

## `internal static LEntry TEntryPrepare(LEngine engine)`

Stores the English entry water with one card, so an editor has something to open.

## `internal static LEditor TEditorPrepare(LEngine engine, string tab)`

Builds an editor over the engine and restores the vista of the named tab, ordered by headword.
