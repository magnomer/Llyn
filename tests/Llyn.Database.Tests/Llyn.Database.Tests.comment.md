# Llyn.Database.Tests.csproj

## `<ItemGroup>`

The engine seeds the controlled vocabularies from the language packs when it binds to a workspace, so the packs have to sit beside the test binaries the same way they sit beside the application's.
