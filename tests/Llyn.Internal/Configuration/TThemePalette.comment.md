# TThemePalette.cs

## `public sealed class TThemePalette`

The theme palette the shell fills its resources from: every colour named, every value a hex colour.
The spare read answers an unknown name with the shipped value, black when none is shipped.
