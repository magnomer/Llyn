# TPortraitSample.cs

## `internal static class TPortraitSample`

One entry likeness every export test shares.

## `internal static LPortraitPage TPortraitSampleCreate()`

It carries one of everything a section can hold, including the awkward cases.
Angle brackets and an ampersand catch a writer that forgets to escape.
A titleless collocation catches a writer that forgets the kind label.
An example row with a gloss, a source and a mention chip catches a writer that skips nested rows.
A hosted video catches the poster frame path.
A usage row under Links here catches a writer that flattens the four parts of an incoming link.
The reading line carries its slashes as marks, so a writer that ignores the marks prints a bare reading.
