# TRequestGloss.cs

## `public sealed class TRequestGloss`

The Gloss requests against a held draft, on a card's sentence row and on the corpus panel's own Example.
Covers the minted negative id, the per-row text and language edits, and the removal.
Also covers the commit that stores the rows in order.
Also covers the in-place update of a shared sentence's Glosses.
