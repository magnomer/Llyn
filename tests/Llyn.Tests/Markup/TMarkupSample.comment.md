# TMarkupSample.cs

## `internal static class TMarkupSample`

One markup document that exercises every element the format knows.
The parse and format tests read it, so a new element is added here once.

## `internal const string TMarkupSampleText`

The sample text: one entry with every row kind, a nested meaning, a collocation and a note.
The reflex row carries a respelling beside its text, as the pronunciation row does.
