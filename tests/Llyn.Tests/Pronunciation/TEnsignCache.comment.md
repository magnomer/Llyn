# TEnsignCache.cs

## `public sealed class TEnsignCache`

The flag cache below the shell: which keys are unasked, which paths are kept, and what a stale age discards.
Every case clears the cache first, since the store is one for the program.
