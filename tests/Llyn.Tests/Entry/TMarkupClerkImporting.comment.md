# TMarkupClerkImporting.cs

## `public sealed class TMarkupClerkImporting`

The markup intake clerk composed over a real rig.

## `public void MarkupClerkImport_PairTree_ReExportsAnEqualTree()`

Two linked entries parsed from markup, imported, and loaded back format to the same markup.

## `private static IReadOnlyList<LMarkupIntake> TMarkupIntakeCreate(int count)`

One fresh intake per parsed entry.
