# TUsher.cs

## `public sealed class TUsher`

The usher's presence check and its refusal to open a folder that is not there.
The successful launch is not driven, since it would open a window on the machine running the tests.
