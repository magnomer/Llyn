# TUsher.cs

## `public sealed class TUsher`

The file usher behind the `LUsher` port: the presence check and the quiet delete.
Opening a folder or a link lives in the media ring, which the tests do not reference.
