# TYunjingLanguage.cs

## `public sealed class TYunjingLanguage`

Covers which language the yunjing panel reads and when its cell page is shown.
A glyph link to a fixture cell opens the page and lists that language's columns and entries.
Clicking the chosen cell again hides the page and empties the entry list.

## `internal static void TYunjingDiweiPlace(LEngine engine, TWorkspace workspace, string language, string character, string initial)`

Stores one character with a placement at the initial and the 寒 rime, anchored to a Korean reflex.

## `internal static LYunjing TYunjingPrepare(LEngine engine)`

A yunjing deportment over the three vistas the window would start, with seams that never ask.
