# TWindowPort.cs

## `public sealed class TWindowPort`

The window deportment over a fake settings port, with no engine and no database behind it.
Each case proves one forwarder hands the veneer's ask to the port unchanged, or normalises where it must.
The first deportment tests written this way, so the ports are shown to be enough for a deportment.
