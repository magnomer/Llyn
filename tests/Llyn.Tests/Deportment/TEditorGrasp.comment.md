# TEditorGrasp.cs

## `public sealed class TEditorGrasp`

Covers the grasp and the favourite over a stored entry.
A grasp write reads back from the engine and words its label.
A fresh draft ignores the write and announces a re-read.
A favourite mark and its clearing read back the same way.
