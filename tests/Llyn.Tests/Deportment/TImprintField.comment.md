# TImprintField.cs

## `public sealed class TImprintField`

Covers the stated fields of the source editor as the deportment forwards them.
A typed title crosses raw and the engine holds it specified, while a blank year holds nothing.
A kind tag round-trips through the draft and takes one chronicle step, and a null tag sends nothing.
A store on an unchanged Source keeps the draft, and one after a change commits and returns to the page.
