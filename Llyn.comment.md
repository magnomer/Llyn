# Llyn.slnx

Lists the seven source projects that `dotnet build Llyn.slnx` compiles.

## Test projects

The projects under `tests` are left out on purpose.
`scripts/test.ps1` finds, builds and runs them from the tests folder instead.
